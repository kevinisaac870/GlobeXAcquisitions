Import-Module ActiveDirectory

$UserList = Import-Csv -Path 'C:\Users\gordn\Downloads\new.csv'

foreach($User in $UserList) {
    $NewUser = @{
        Name = $User.first + " " + $User.last
        GivenName = $User.first
        SurName = $User.last
        SamAccountName = $User.first + " " + $User.last
        Department = $User.department
        Title = $User.title
        UserPrincipalName = $new.GivenName.Substring(0,1) + $new.Surname + "@globex.com"
        Path = "OU=Employees, DC=yourdomain, DC=com"
        Enabled = $true
        }
    Write-Output $NewUser
}

# TODO: Assigns the Windows Server VM a static IPv4 address
# TODO: Assigns the Windows Server VM a DNS
# TODO: Renames the Windows Server VM
# TODO: Installs AD-Domain-Services
# TODO: Creates an AD Forest
# TODO: Creates Organizational Units (OU)
# DONE: Takes a CSV files of users and creates AD users and adds them to the appropriate OUs